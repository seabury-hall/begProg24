# Get the user's input and convert the input strings to ints
x = input("Enter a whole number: ")
y = input("Enter another whole number: ")

# Make sure addition works as we expect it to
z = x + y

# Print the result
print(z)

# Modify below to print the formatted result with commas (if over 999)
print(f"{z}")



# Get the user's input and cast (convert) the input strings to floats
numerator = input("Enter a decimal number: ")
denominator = input("Enter another decimal number: ")

# Calculate the result of dividing the two numbers and store in variable quotient
quotient = ''

# Print the result
print(f"Result: {quotient}")

# Modify below to use the round function to print the result rounded to two places
print(f"Result Rounded: {quotient}")

# Modify below to use the formatter to print result with only two decimal places
print(f"Result to two decimals: {quotient}")